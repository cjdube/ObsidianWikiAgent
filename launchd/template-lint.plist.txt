<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Reference template for a vault's weekly audit job. Not itself loaded by
     launchd — launchd/install.sh fills in the placeholders and writes the real
     <label>.plist (concrete .plist files are gitignored, so they stay local to
     your machine).

     Optional: the audit is worth scheduling once a vault is big enough that
     reading every page by hand stopped being realistic. Until then, run
     wiki_lint.py when you feel like it.

     This job passes no fix flag, so it only reads the vault — safe to run
     unattended, and the report lands in the log for a human to act on.
     (wiki_lint.py can write, but only under that flag, and only the two
     mechanical fixes; see its module docstring.)

     The flag is spelled with two dashes, but this comment cannot say so
     literally: XML forbids a double dash inside a comment. plutil and launchd
     accept it anyway, so the only thing that breaks is a strict XML parser —
     LocalLLMAgent's dashboard reader dropped a job's row until it was reworded.

     Runs Sunday at 10:00, after that morning's 09:00 ingest, so it audits a
     full week of new pages.

     Placeholders install.sh replaces:
       <ABSOLUTE_PATH_TO_REPO>  where you cloned this repo
       <VAULT_NAME>             short name for the vault (e.g. learnings)
       <ABSOLUTE_PATH_TO_VAULT> the vault directory the job is pointed at
       <VAULT_BASENAME>         the LAST PATH COMPONENT of that vault directory

     The last two are usually different, and the difference is why WREN_RUN_LOG
     used to name a file that never existed: VAULT_NAME is a short label you
     pick, while setup_logger names the structured log after the vault
     directory's own basename. install.sh derives both, so run it rather than
     filling these in by hand. -->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>local.wikiagent.<VAULT_NAME>-lint</string>

    <key>ProgramArguments</key>
    <array>
        <string><ABSOLUTE_PATH_TO_REPO>/.venv/bin/python</string>
        <string><ABSOLUTE_PATH_TO_REPO>/wiki_lint.py</string>
        <string>--vault</string>
        <string><ABSOLUTE_PATH_TO_VAULT></string>
        <string>--deep</string>
    </array>

    <key>WorkingDirectory</key>
    <string><ABSOLUTE_PATH_TO_REPO></string>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key>
        <integer>0</integer>
        <key>Hour</key>
        <integer>10</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>StandardOutPath</key>
    <string><ABSOLUTE_PATH_TO_REPO>/logs/<VAULT_NAME>-lint.launchd.log</string>
    <key>StandardErrorPath</key>
    <string><ABSOLUTE_PATH_TO_REPO>/logs/<VAULT_NAME>-lint.launchd.log</string>

    <!-- LLM_PROVIDER is deliberately absent, so this job inherits the local
         ollama default the way the daily ingest does. This template set gemini
         until 2026-09-10, and the reason it no longer does is not that Gemini
         audits badly: it is that a tracked file should not ship a default which
         sends every page of whatever vault you point it at to a third party the
         first time somebody runs install.sh. A privacy decision belongs to the
         person installing the job, so this template makes them type it.

         The deep pass is still the one job where model quality decides whether
         the findings are worth reading, and local is not a free swap. On a
         frozen vault copy seeded with 12 planted defects, Gemini named 4 and
         the local model 2 (docs/agent-context.md has the run). A working
         audit, not an equal one.

         To send this job's reading to Gemini, add the two lines below to the
         dict that follows and re-run launchd/install.sh, which prints a privacy
         warning naming the provider. GEMINI_API_KEY comes from config/.env via
         load_dotenv, never from this file. Read SECURITY.md first.

             <key>LLM_PROVIDER</key>
             <string>gemini</string>

         OLLAMA_MODEL is also absent, and for the same kind of reason: a tag
         that is not pulled on the machine makes the job fail, so a tracked
         template must not assume one. But the deep pass is the one job where
         the tag matters, and config/.env's ingest default is the wrong pick
         for it. gemma4:26b-mlx is a mixture-of-experts model: 25.2B total
         parameters, only ~3.8B active per token. It audits a whole wiki with
         about 4B parameters of judgment, and it shows — it answers in three
         grouped findings and stops.

         Measured 2026-09-10 on a 35-page fixture with 12 planted defects,
         three trials each (tools/compare_lint_models.py rebuilds this):

             qwen3.8:27b-mlx   9.7 of 12   0 false findings   397s
             gemma4:26b-mlx    5.0 of 12   0 false findings   105s
             gemma4:31b-mlx    4.7 of 12   1 false finding    222s

         qwen3.8:27b-mlx is dense and roughly twice the auditor for about four
         times the wall clock, which still lands well inside the 30-minute
         budget. The dense Gemma is not worth it: same recall, twice the time.
         All three stayed 100% GPU at OLLAMA_NUM_CTX=65536 on a 48 GB machine.

         To use it, pull the tag (`ollama pull qwen3.8:27b-mlx`) and add the
         two lines below to the dict that follows. agent/__init__.py calls
         load_dotenv without override, so a real environment variable set here
         beats config/.env and the daily ingest keeps its own model.

             <key>OLLAMA_MODEL</key>
             <string>qwen3.8:27b-mlx</string>

         WIKI_LAUNCHD_LOG: same path as StandardOutPath above, so the script can
         cap the file it is writing to. launchd owns that descriptor and nothing
         rotates it otherwise. Must match those two paths exactly.

         WREN_RUN_LOG: optional, and only useful if you also run LocalLLMAgent —
         its dashboard reads run history from the STRUCTURED log named here,
         which setup_logger names after the script and vault, not after
         StandardOutPath. Drop the key if you don't run it. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>WIKI_LAUNCHD_LOG</key>
        <string><ABSOLUTE_PATH_TO_REPO>/logs/<VAULT_NAME>-lint.launchd.log</string>
        <key>WREN_RUN_LOG</key>
        <string><ABSOLUTE_PATH_TO_REPO>/logs/wiki_lint.<VAULT_BASENAME>.log</string>
    </dict>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
